<!DOCTYPE html>
<html>
<head>
    <title>Login Page</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Monospace;
            background-color: #EABF9F;
        }

        .login_container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            background-color: #B68973;
            width: 80%;
            max-width: 500px;
            padding: 20px;
            border: 2px solid #1E212D;
            border-radius: 10px;
        }

        .login_container h1 {
            color: #1E212D;
            margin-bottom: 20px;
            font-size: 2em;
        }

        .login_container form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .login_container input {
            width: 100%;
            padding: 15px;
            margin-bottom: 15px;
            box-sizing: border-box;
            background-color: #EABF9F;
            border-radius: 20px;
            border: 2px solid #1E212D;
            font-size: 1.2em;
        }
        .login_container input:focus {
            outline: none;
        }

        .login_container input[type="submit"] {
            background-color: #1E212D;
            color: white;
            cursor: pointer;
            border-radius: 25px;
            border: 2px solid #1E212D;
        }

        .login_container input[type="submit"]:hover {
            background-color: #EABF9F;
            color: black;
        }

        .login_container a {
            text-decoration: none;
            color: #EABF9F;
            font-weight: bold;
            font-size: 1.2em;
        }
    </style>

</head>
<body>
<div class="login_container">
    <h1>Prijavi se</h1>

    <ul>
        <% if(messages.success_msg){ %>
            <li><%= messages.success_msg %></li>
        <%}%>
    </ul>

    <ul>
        <% if(messages.error){%>
        <li><%=messages.error%></li>
        <%}%>
    </ul>

    <form action="/login" method="post">
        <div>
            <input type="text" id="korisnicko_ime" name="korisnicko_ime" placeholder="Korisničko ime" required>
        </div>
        <div>
            <input type="password" id="lozinka" name="lozinka" placeholder="Lozinka" required>
        </div>
        <div>
            <input type="submit" value="Prijavi se">
        </div>
        <a href="/register">Registruj se ovdje</a>
    </form>
</div>
</body>
</html>

